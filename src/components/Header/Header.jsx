import { HeaderContainer, HeaderText } from "./Header.styled";

export const Header = () => {
  return (
    <HeaderContainer>
      <HeaderText>Twitter. It's what's happening</HeaderText>
    </HeaderContainer>
  );
};
